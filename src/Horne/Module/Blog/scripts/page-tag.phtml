<?php

namespace Horne;

/* ---
{
    "id"     : "horne-layout-page-tag",
    "type"   : "layout",
    "layout" : "horne-layout-default"
}
--- */

/**
 * @var Api   $api
 * @var array $vars
 */

$m = $vars['meta'];

$metas = $api->getMetasByTag($m['tag']);

?>

<h1>All entries tagged with <?=$api->e($m['tag'])?></h1>

<?=$api->render('horne-blog-sub-article-list', array('articles' => $metas))?>
